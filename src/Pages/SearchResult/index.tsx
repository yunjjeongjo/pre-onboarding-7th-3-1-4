import styled from "styled-components";

const SearchResult = () => {
  return <Title>검색 결과는 준비중입니다!</Title>;
};

const Title = styled.h1`
  margin-top: 50px;
  font-size: 30px;
  color: #fafafa;
  font-weight: 700;
`;

export default SearchResult;
